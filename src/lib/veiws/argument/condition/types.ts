import type { Condision, StatementType } from "$lib/state/arrayData/types"

export type Props={
    condition:Condision
    statementType:StatementType
    methodIndex:number
    arrgIndex:number
    statementIndex:number
    conditionIndex:number
    type:string
}